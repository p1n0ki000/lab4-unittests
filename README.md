# Лабораторная работа №4 — Unit-тесты в Python

> *«Если это не покрыто тестами — считай, что этого не существует.»*  

Проект представляет собой реализацию простого модуля для вычисления площади и периметра прямоугольника, а также набор модульных тестов на базе стандартной библиотеки `unittest`.

---

## 📌 Цели проекта

- Показать на простом примере, как писать **unit-тесты** на Python.
- Научиться использовать **`unittest`** для автоматизации тестирования.
- Организовать проект так, чтобы:
  - код и тесты были разнесены по разным файлам;
  - тесты запускались одной командой из консоли;
  - репозиторий можно было легко использовать как шаблон.

---

## 🧩 Функциональность

Модуль `rectangle.py` содержит две основные функции:

- `area(a, b)`  
  Вычисляет **площадь прямоугольника** по двум сторонам `a` и `b`.

- `perimeter(a, b)`  
  Вычисляет **периметр прямоугольника** по двум сторонам `a` и `b`.

Функции работают с целыми и вещественными числами.

---

## 🗂 Структура проекта

```text
.
├── rectangle.py        # Основной модуль с реализацией функций area и perimeter
├── test_rectangle.py   # Набор модульных тестов (unittest)
└── README.md           # Описание проекта
